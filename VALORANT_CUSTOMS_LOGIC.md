# 🧠 Логика работы: Valorant Customs

## 1. Инициализация
- Пользователь вызывает слэш-команду `/custom` **только** в текстовом канале **📋-создание-кастомок** (в других каналах — отказ, эфемерное сообщение с ссылкой на канал).
- Бот отправляет сообщение с пингом роли `@valorant` и Embed с двумя кнопками: **[RANDOM]** и **[VOTED]**.
- Пользователь, нажавший кнопку, закрепляется системой как **Хост**.

## 2. Комната ожидания
- Бот создает голосовой канал `Ожидание (Номер лобби)`.
- Бот обновляет Embed: *"Ожидание готовности игроков"*.
- Появляется кнопка **[ВСЕ ГОТОВЫ]**.
- **Условие:** Нажать может ТОЛЬКО Хост. Если жмет другой игрок — получает эфемеричное (желтое, видимое только ему) сообщение: *"Вы не организатор, ожидайте @Хост"*.
- **Валидация при нажатии:** Бот проверяет ГК "Ожидание". Игроков должно быть четное количество. Бот проверяет наличие ролей и заносит новых игроков во внутреннюю БД (JSON).

## 3. Настройка матча
После успешного нажатия "ВСЕ ГОТОВЫ", Embed меняется на Dashboard с информацией об игроках. Логика расходится.

### Вариант А: RANDOM
Появляются кнопки: `[TEAM]`, `[MAP]`, `[AGENTS]`.
- **TEAM:** Балансирует игроков на 2 команды с учетом их рейтинга из БД. Показывает стороны старта (Атака/Защита).
- **MAP:** Выбирает 3 случайные карты из пула. Создает 3 кнопки с названиями этих карт. Выбирает Хост.
- **AGENTS:** Абсолютный рандом агентов для каждого игрока.
  - Под общим Эмбедом висит кнопка `[REROLL]`. Лимитов на реролл нет.
  - Если игроку выпадает закрытый агент, он жмет `[REROLL]`. Бот определяет, кто нажал, и перезапускает рандомайзер лично для него, обновляя общий Эмбед.

### Вариант Б: VOTED
Появляется кнопка `[TEAM]`.
- **TEAM:** Балансирует команды (как в RANDOM). 
- **ВЫБОР КАПИТАНОВ:** 
  - Каждому участнику присылается личное/эфемеричное сообщение с выпадающим списком (Dropdown) игроков ЕГО команды.
  - Идет тайное голосование. Можно голосовать за себя.
  - При ничьей или фулл-соло голосах Капитан выбирается рандомом из лидеров.
  - Капитан 1 получает доступ к *зеленым* кнопкам интерфейса, Капитан 2 — к *красным*. Полномочия Хоста остаются приоритетными.
- **MAP:** Формируется выпадающий список (Dropdown) со всеми доступными картами. Капитаны по очереди выбирают карты из списка для бана. Забаненные карты пропадают из списка, пока не останется одна.
- **AGENTS:** Рандом агентов, но по строгим пресетам ролей.

## 4. Старт матча
- Все настройки завершены. Бот пишет: *"Матч начнется через 10 секунд"*.
- Бот создает ГК `Команда 1` и `Команда 2`.
- Бот перетягивает (move) игроков из "Ожидания" в каналы их команд.
- Бот удаляет ГК "Ожидание".
- Бот пишет итоговое сообщение в чат с пингом всех участников и пожеланием *GL HF*.

## 5. Завершение матча
- Хост пишет слэш-команду `/stop`.
- Бот открывает перед Хостом **Модальное окно** (Pop-up форму прямо в интерфейсе Discord).
- В модальном окне будут текстовые поля (Например: "Победитель (Команда 1 / Команда 2)", "Счет Команды 1", "Счет Команды 2").
- Хост заполняет форму и жмет "Отправить".
- Бот обрабатывает данные, начисляет победы/поражения в БД и закрывает сессию матча.